import { TimeGonePipe } from './time-gone.pipe';

describe('TimeGonePipe', () => {
    it('create an instance', () => {
        const pipe = new TimeGonePipe();
        expect(pipe).toBeTruthy();
    });
});
