import { PercentageWidthPipe } from './percentage-width.pipe';

describe('PercentageWidthPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentageWidthPipe();
    expect(pipe).toBeTruthy();
  });
});
